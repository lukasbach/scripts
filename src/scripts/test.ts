/** @internal */

// await ask.text("_", "positional 1");
// await ask.text("_", "positional 2");
// await ask.text("t,text", "Text input");
// await ask.number("n,number", "Number input");
await ask.bool("b,bool", "Boolean input");
// await ask.confirm("c,confirm", "Confirm input");
// await ask.multiChoice("m,multi", "Multi choice input", ["a", "b", "c"]);
// await ask.choice("c,choice", "Choice input", ["a", "b", "c"]);
// await ask.path("path", "Path input");
// await ask.editor("editor,e", "Editor");
